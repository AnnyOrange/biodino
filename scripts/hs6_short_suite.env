# Shared H-S6 short suite (26 tasks, no detection). Source before eval launchers.
export TASKS="${TASKS:-classification regression retrieval segmentation}"
export CLASSIFICATION_DATASETS="${CLASSIFICATION_DATASETS:-bloodmnist pathmnist tissuemnist breastmnist organamnist organcmnist organsmnist dermamnist octmnist pneumoniamnist retinamnist chestmnist pcam nct-crc-he lc25000 bbbc048-cellcycle}"
export REGRESSION_DATASETS="${REGRESSION_DATASETS:-bbbc013 bbbc005}"
export RETRIEVAL_DATASETS="${RETRIEVAL_DATASETS:-lc25000 nct-crc-he-100 nct-crc-he-1k crc-val-he-7k}"
export SEGMENTATION_DATASETS="${SEGMENTATION_DATASETS:-bbbc038 conic monuseg pannuke}"
export CONCURRENT_TASK_GROUPS="${CONCURRENT_TASK_GROUPS:-0}"
export PRUNE_COMPLETED_CACHE="${PRUNE_COMPLETED_CACHE:-0}"
export FROZEN_DATASETS_PER_JOB="${FROZEN_DATASETS_PER_JOB:-1}"
export NUM_WORKERS="${NUM_WORKERS:-1}"
export SEG_FEATURE_NUM_WORKERS="${SEG_FEATURE_NUM_WORKERS:-1}"
export SEG_PROBE_NUM_WORKERS="${SEG_PROBE_NUM_WORKERS:-1}"
