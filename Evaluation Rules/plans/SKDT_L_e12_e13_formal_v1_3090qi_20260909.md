# SKDT-L e12/e13 Formal-v1 Campaign

状态：`RUNNING`，启动时间 2026-09-09 09:11–09:12 UTC。

## 研究问题

在完全 matched 的自监督下游评测协议下，比较 HS6-L baseline、matched SKDT stable-delta 和 shuffled-control stable-delta。当前先评测已完整保存的 e12/e13；e14/e15 checkpoint 生成后进入同一 protocol 的后续 manifest，不混入当前进程。

## 模型与 checkpoint

- matched：`outputs/01_training_runs/HS6_L_e11to15_SKDT_stable_w0.5_bs64_gb1024_seed0_2x3090qi_20260908/ckpt/{12299,13324}/checkpoint.pth`
- shuffled：`outputs/01_training_runs/HS6_L_e11to15_SKDT_shuffled_stable_w0.5_bs64_gb1024_seed0_2x3090qi_20260908/ckpt/{12299,13324}/checkpoint.pth`
- baseline：`outputs/01_training_runs/HS6_L_robust_biosafe256_gb1024_lr1e4_wu3_tw30_nosig_e15_seed0_20260818/ckpt/{12299,13324}/checkpoint.pth`
- 权重分支：teacher/EMA。

Baseline 的 non-dense 历史结果先由 validator 核对后复用；PanNuke/CoNIC 因 split 已改变必须重测。若任一历史 cell 的 batch、resolution、feature、seed、split 或 code protocol 不一致，则只重测该 cell，不复用旧 aggregate。

## 固定协议

- 机器可读规则：`Evaluation Rules/protocol_v1.json`
- frozen classification/regression/retrieval：batch 64，224，final CLS + final patch mean，L2 normalize，seed 0。
- segmentation：dataset-specific resolution/resize；ViT-L even4=`[4,11,17,23]` 或 last1=`[23]`；feature batch 32，probe batch 32，50 epochs，只在 epoch 50 eval。
- detection：batch 8，5 epochs，seed 0。
- 禁止：LC25000、NCT-100、smoke/max_samples、open-set CHAMMI closed-set probe。
- PanNuke：三个互斥官方 fold protocol 全跑并最终取均值。
- CoNIC：`official-baseline-fold0-nested-v1`，公开 test 字段解释为 development holdout。

## 完整矩阵

每个 arm、每个 checkpoint：24 classification、4 regression、4 retrieval/clustering、8 segmentation、3 detection。Tier A 与 Tier B 分别聚合。OOD xray/cryo 使用专用 evaluator 和同一全局 feature protocol，另建 manifest，不塞进本 launcher。

## 机器与并发

- 执行机：3090-qi；checkpoint、dataset、output 均直接读写 Huawei 共享盘，没有 checkpoint/data 互传。
- matched：GPU 2/5，3 个测试进程/卡；launcher PID 1392920（shell PID 1392919）。
- shuffled：GPU 6/7，3 个测试进程/卡；launcher PID 1393133（shell PID 1393132）。
- 训练仍占 GPU 0/1/3/4，不改变训练进程。
- 启动后 GPU 2/5/6/7 显存约 7.3/8.9/10.0/7.3 GiB，均低于 60%，利用率 100%。

## 固定代码与输出

- Git commit：`8529307ec`（已推送 `origin/main`）。
- 干净 runtime：`outputs/02_eval_runtime/bio_eval_formal_v1_8529307`。
- output：`outputs/02_eval_runs/skdt_formal_v1_3090qi_20260909/{matched,shuffled}`。
- launcher log：同一 campaign root 下的 `matched_launcher.log`、`shuffled_launcher.log`。
- 两个 arm 的 `command_manifest.json` 均为 86 jobs，preflight 均为 PASS。

## 完成与清理

进程退出 0 仅代表运行结束；所有 cell 还需 result validator 检查参数和结果完整性。旧 batch16/Ret4 输出保持 `INVALID_PROTOCOL`，本轮不删除任何旧 output。campaign 完成后生成全机/output 只读清单，再由用户决定清理范围。
